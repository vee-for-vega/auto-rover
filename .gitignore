# ROS 2 / colcon build artifacts (regenerated by `colcon build` — never commit)
build/
install/
log/

# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]

# Editors / IDEs
.vscode/
.idea/
*.swp

# Terraform (cloud infra — later modules)
*.tfstate
*.tfstate.*
.terraform/
